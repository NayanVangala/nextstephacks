node_modules/
dist/
__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/
src/backend/.cache/

# secrets —— anon key 虽为 public,亦不硬写于码
.env
.env.local
src/frontend/.env
src/frontend/.env.local

# chroma 之索,可再建,不入库
src/backend/.chroma/
.gstack/

# playwright MCP 之遺:每run所存之 snapshot 與 console,非source
.playwright-mcp/

# 前後之圖,九兆有餘 —— 入之則永在其史,而其所載者已在其 README。
# 圖存於其地,不入其庫。
docs/design/**/before/
docs/design/**/after/

# QA 之圖,十九兆 —— 與上同理,存於其地而不入其史。
shots/
