# Supabase。无此二者,则 app 入 local-only 之模 —— 此非坏,routing 本不赖之。
# Without these the app runs local-only: reports stay in browser SQLite and the
# UI says "not synced". Routing never depended on the network and still does not.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
